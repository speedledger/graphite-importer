graphite = {
    url = "http://example.com"
    authorization-credentials = "username:password" // Skip if Basic authentication is not used
}

elasticsearch = {
    url = "http://example.com"
    authorization-credentials = "username:password" // Skip if Basic authentication is not used
}

updater = {
    interval = 1 minutes
    last-updated-time-file = "lastUpdatedTime" // The last updated time is saved to file, which is read when the application restarts
    jenkins = {
        enabled = true
        timeAdjustment = 1 minutes
        querySize = 1000
    }
}

akka {
  loggers = ["akka.event.slf4j.Slf4jLogger"]

  loglevel = INFO
}
